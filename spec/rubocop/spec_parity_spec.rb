# frozen_string_literal: true

RSpec.describe Rubocop::SpecParity do
  it "has a version number" do
    expect(Rubocop::SpecParity::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
